apiVersion: dapr.io/v1alpha1
kind: Component
metadata:
  name: statestore
spec:
  type: state.postgresql
  version: v2
  metadata:
    - name: connectionString
      value: "host=localhost user=postgres password=ucprulz! port=5432 connect_timeout=10 database=dapr"
    - name: actorStateStore
      value: "true"